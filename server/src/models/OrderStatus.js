

const ORDER_STATUS = {
    ORDER_PLACED: "Bestellung abgegeben",
    PROCESSING: "Bestellung in Bearbeitung",
    SHIPPED: "Versendet",
    COMPLETED: "Zugestellt"
};

const orderKeys = Object.keys(ORDER_STATUS);


export default ORDER_STATUS;
 export {
     orderKeys
 };

